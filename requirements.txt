# Core runtime dependencies for SAF-W.
# Versions pinned to match the TriMix/DExperts fork the decoder is built on.
# transformers 4.47.0 is required: the cached generation loop uses the
# _supports_cache_class hack and prepare_inputs_for_generation interface from
# this version. Newer transformers (4.50+) change these and break KV caching.
torch==2.5.1
transformers==4.47.0
accelerate>=0.30
sentencepiece>=0.1.99
numpy>=1.24
sacrebleu>=2.4
# ROUGE: MiLiC-Eval uses the multilingual fork (xl-sum), NOT the official
# rouge-score package. The fork installs AS rouge_score and overrides it, so
# the official package must not be listed here. pyonmttok is its tokenizer.
pyonmttok>=1.38
rouge_score @ git+https://github.com/csebuetnlp/xl-sum.git#subdirectory=multilingual_rouge_scoring
tqdm>=4.65
