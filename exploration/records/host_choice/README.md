Host-selection scores on alternative selection sets (RC, RS).
t1rest_* and t23_*: train1-rest and train2+3 with train1 exemplars (7B/14B).
*_train1ex_*: the same protocol at 32B.
These back the selection-set robustness ablation. The deployed protocol uses devx (see results/devx).
